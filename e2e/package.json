{
  "name": "watch-e2e",
  "private": true,
  "description": "Post-deploy functional smoke (Playwright) — runs locally (make e2e) and as the pipeline's final gate against staging.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
